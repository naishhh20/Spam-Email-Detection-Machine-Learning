# Spam-Email-Detection-Machine-Learning
This project implements a spam email detection system using machine learning. It combines Logistic Regression, SVM, and a Stacking Ensemble for enhanced accuracy. The pipeline includes text preprocessing, TF-IDF feature extraction, and model evaluation, providing a robust solution for filtering spam emails.
